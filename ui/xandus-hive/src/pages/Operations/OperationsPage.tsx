/**
 * Operations Page — Hive Control (live WebSocket agent management)
 */

import React from 'react';
import { HiveControlPanel } from '@/components/Operations/HiveControlPanel';

export const OperationsPage: React.FC = () => <HiveControlPanel />;

export default OperationsPage;
